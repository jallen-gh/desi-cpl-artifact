# desi-cpl-artifact

This repository contains the Python code required to reproduce the semi-analytic mock data fit and integral degeneracy calculations presented in the phenomenological letter: **"The DESI Phantom Crossing as a Parameterization Artifact: A Logarithmic Baseline and Falsifiable Predictions for DR3."**

## Overview
Recent constraints from DESI DR2 suggest an apparent "phantom crossing" in the dark energy equation of state when analyzed using the standard Chevallier-Polarski-Linder (CPL) parameterization. This code demonstrates that a rigid linear parameterization attempting to fit integral distance data generated by a strictly monotonic, logarithmically evolving phantom baseline will artificially contort. 

The script `mock_fit.py` evaluates the CPL least-squares minimization across three progressive scenarios:
1. **Pure Noiseless Baseline:** Proves the underlying geometry naturally recovers a flat projection.
2. **Realistic Local Variance:** Demonstrates that standard 1.5% noise triggers a moderate phantom crossing to satisfy high-z integral boundaries.
3. **Systemic Dataset Tension:** Simulates the known 3-4.5% distance anomalies observed in specific DESI LRG bins, violently forcing the CPL model into the exact $w_0 \approx -0.75$, $w_a \approx -1.17$ "crisis" regime reported in recent literature.

## Requirements
* `numpy`
* `scipy`

## Usage
Run the script directly from the command line:
```bash
python mock_fit.py
